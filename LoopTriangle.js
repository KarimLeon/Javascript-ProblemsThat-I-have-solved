var y = "" //Created a variable, then gave it a empty string value which equals the Boolean Value "False"!

for(var x = 1; x <= 7; x++) {  //Variable X Initialized the loop, 2nd statement checks whether it should keep going so if it's true, it'll move on, and 3rd statment increments x by 1.

    y += "#"
	
    console.log(y);
    
};
